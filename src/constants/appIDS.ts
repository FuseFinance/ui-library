export class DefaultComponentIds {
  static readonly fuseTooltipContainer = `${this.name}_fuseTooltipContainer`;
}

export class ComplexInputIds {
  static readonly SimpleInputBody = `${this.name}_SimpleInputBody`;
  static readonly FunctionalCodeInputBody = `${this.name}_FunctionalCodeInputBody`;
}
