import { Meta, Canvas } from '@storybook/addon-docs';
import BaseCodeEditor from '../BaseCodeEditor/baseCodeEditor';
import { javascript } from '@codemirror/lang-javascript';
import ComplexInput from '@components/ComplexInput';
import { FUNCTIONAL_INPUT_TYPE } from './types';

<Meta title="Form/ComplexInput" component={ComplexInput} />

# Complex Input

## Complex Input Import

```jsx
import { ComplexInput } from '@fuse-finance/ui-library';
```

## Complex Input Simple

<Canvas>
  <ComplexInput.Simple
    onChange={console.log}
    value="var myVar = customVars.myOpntionOne()"
    placeholder="my placeholder"
  />
</Canvas>

## Complex Input Functional Code

<Canvas>
  <ComplexInput.FunctionalCode
    hasNumberLines={true}
    defaultValue={
      `
      const myVar = customVars.myOptionOne()
      const user = {
        name: 'TEST'
      }
      2 + 2 = 4
      `
    }
    onChange={console.log}
    onBlur={console.log}
    customCSSClass=""
    extensions={[]}
    plugins={[
      {
        name: 'customVars',
        isModule: true,
        decorationClass: 'code-tag code-editor-hl-formula',
        options: {
          'myOpntionOne()': 'customVars',
        },
        matchRegex: /customVars\.(myOpntionOne())\b\((.*?)\)/g,
      },
    ]}

/>

</Canvas>
## Complex Input Functional Code without numbers

<Canvas>
  <ComplexInput.FunctionalCode
    defaultValue={
      `2 + 2 = 4

      const user = "Jhon Doe"

      // More content here...

      const sayHelloFunc = (name) => ('Hey 👋🏻, ' + name)
      `
    }
    type={FUNCTIONAL_INPUT_TYPE.FORMULA}
    onChange={console.log}
    onBlur={console.log}
    customCSSClass=""
    extensions={[]}
    plugins={[
      {
        name: 'customVars',
        isModule: true,
        decorationClass: 'code-tag code-editor-hl-formula',
        options: {
          'myOpntionOne()': 'customVars',
        },
        matchRegex: /customVars\.(myOpntionOne())\b\((.*?)\)/g,
      },
    ]}

/>

</Canvas>
